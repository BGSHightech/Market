package com.bgs.market.application.role.persistence;

import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Class for RoleRepository.
 */
public interface RoleRepository extends JpaRepository <Role, Long> {
}
